import {default as request, GET, POST, PUT, DELETE} from '@/utils/request'
import baseApi from '@/api/base'

const baseUrl = '/rest/zhaopinMember'

export default Object.assign(baseApi(baseUrl), {
})
